@model AlertMessage

<div class="row mt-3">
    <div class="col-md-12">
        <div class="alert alert-@Model.AlertType">
            <h4 class="alert-title">@Model.Title</h4>
            <p>@Html.Raw(Model.Message)</p>
        </div>
    </div>
</div>